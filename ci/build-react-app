#!/bin/bash

set -o nounset -o errexit

docker run --rm -it \
  -v "$(pwd):/app" \
  --workdir /app \
  node:latest \
  npm build

# npm build

echo "💚 Build succesful on $(hostname):$(pwd)"

ls -las